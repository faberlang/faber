# file header comment
functio computa(numerus x) → numerus {
    # inner comment before statement
    varia numerus y ← x + 1

    # comment between statements
    redde y * 2
}

functio vacuus() → numerus {
    # only a comment
    redde 0
}
