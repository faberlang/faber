# Archived factory docs

Closed-out factory goals and campaigns move here from `docs/factory/` when
they are **completed, superseded, or will not be worked in the future**.
Use `git mv` so history follows. Live/planned/deferred goals stay in
`docs/factory/`.

Nothing in this directory is active work — it is record-only.
