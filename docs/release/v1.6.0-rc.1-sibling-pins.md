# Faber 1.6.0-rc.1 sibling pins (container tips at lock refresh)
#
# RC candidate pin record (Stage 7 TR7-U1, gpu-training-lowering campaign).
# The RC pair must be buildable: faber `6dbda59` (RC base; the release bump
# commit is its child) pins against radix `4792fcf6` — resolving stage-6
# residual 3 (faber `887735be3` was API-incompatible with radix main because
# the `Target::Rust`→`HirRust` / `radix::tool` postprocess renames landed
# after that pairing; faber has since adopted the renamed API in `844c4de`
# "refactor: follow radix target variant names" + `5f0f95c` "refactor: own
# target postprocessing in faber", so current faber main pairs with current
# radix main). These SHAs match the tree used to regenerate `Cargo.lock` and
# the `release-manifest.yaml` for the 1.6.0-rc.1 candidate. Proven by the
# locked release build (`cargo build --locked --release --bin faber`) and the
# compiled-package archive. No tag this stage; publication is Stage 8.

| Repo | Full SHA |
| --- | --- |
| faber | `6dbda59b2b838385900a3e469aa57a88cdb0350d` |
| radix | `4792fcf6acb115336697a948c6848dde9e39378e` |
| faber-runtime | `10d48ea474358e6a6d36e54d56f051ae20b3824f` |
| hosts | `e066ee0ae98afa5c7556e1f765072a6357050149` |
| cista | `935528820653f0b3cd28e63c9d21ac7db90add65` |
| gradus | `24e54989f5b2044c2daf5c15b6a7e7b495b129b5` |
| examples | `aad199ecf07cb23f5d4127c3f68974cab3901235` |

The hosts entry is the unified monorepo (kernel `crates/host-kernel`, native
`crates/host-native`, providers `crates/provider-contracts`; the 1.1.1-era
separate `host-kernel-rs` / `host-native-rs` / `host-providers-rs` repos are
no longer split). `gradus` and `examples` are recorded here for the Stage 7
fixture provenance; the release manifest pins the five build inputs (faber,
radix, cista, faber-runtime, hosts) per `release-manifest-schema.md` §4.

## Dirty-state declaration (`git status --short`, at pin time)

All companion repos clean except: `radix` carries one untracked planning
artifact (`docs/factory/gpu-training-lowering/stage-7-delivery.md` — the
Stage 7 delivery spec, owned by planner-3/Mind, not part of the build). No
repo had tracked modifications at the lock refresh.

Note: radix main tip moved once during the unit (`2adb2c10` → `4792fcf6`,
docs-only GI3-7 evidence commit, no crate-source change) — the pin records
the final tip `4792fcf6`; the locked build compiled the identical radix crate
sources at both tips.

If the `--locked` build breaks at a later refresh, either re-pin these SHAs in
`release.yml` or refresh the lock against new tips and cut a new candidate.

## Per-target archive hashes (CTO-5 follow-up, TR7-Linux)

One canonical FMIR semantic identity across targets (the `packages/*/image.fmir`
bytes are identical in both archives); distinct per-target launcher binary +
archive hashes.

| Target triple | Launcher binary sha256 | Archive sha256 |
| --- | --- | --- |
| `aarch64-apple-darwin` | `8b45f0f7d193b62f59538cc7e870c4538946090cd620fcb669815df05c9a0eb4` | `06742c02e7d3972c9694c769f4674906063509874bef9fd04b7fd3f020332566` |
| `x86_64-unknown-linux-gnu` | `0176ca689a2d3d191a9405f8640a257802aa9a5615341afa6daaeaaa01a4280f` | `13379f3e7442979c80fe89d3e4dda4fe3de590c9892400a6fb4cf632cf090192` |

The `aarch64-apple-darwin` launcher digest matches the `release-manifest.yaml`
launcher pack digest (TR7-U1). The `x86_64-unknown-linux-gnu` archive was built
from this same pinned tree (faber `329ad04` RC-lock tree; radix `4792fcf6`,
cista `93552882`, faber-runtime `10d48ea`, hosts `e066ee0a`) in an amd64
`ubuntu:24.04` container on burgus (Docker, glibc 2.39 — matches pharos) with
rustc 1.97.1 (same release/commit as the TR7-U1 macOS build).
`cargo build --locked --release --bin faber` green; `faber --version` →
`faber 1.6.0-rc.1`; ELF 64-bit x86-64, glibc-linked (libc 2.39).
