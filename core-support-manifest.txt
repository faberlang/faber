# Logical roots embedded by the Faber RC core-support payload.
# Paths are relative to the faberlang container (parent of the faber crate).
# Host libraries live under hosts/crates/; each crate uses explicit path deps
# so the archive does not need the hosts workspace root.
faber-runtime
hosts/crates/host-kernel
hosts/crates/host-native
hosts/crates/aleator
hosts/crates/http
hosts/crates/consolum
hosts/crates/processus
hosts/crates/solum
hosts/crates/tempus
